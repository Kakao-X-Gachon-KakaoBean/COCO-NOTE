export interface IProjectList {
    title: string
}

export interface IProjectValue {
    projectName: string,
    projectExplain:string,
    noteList: string[],
    todoList: string[],
    issueList: string[],
}
